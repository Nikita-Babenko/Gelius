DROP TABLE IF EXISTS CLIENTS;
DROP TABLE IF EXISTS PRODUCTS;
DROP TABLE IF EXISTS ORDERS;
DROP TABLE IF EXISTS REQUESTS;


CREATE TABLE CLIENTS (
  CLIENTS_ID   BIGINT NOT NULL AUTO_INCREMENT,
  ADDRESS      VARCHAR(200),
  COMPANY_NAME VARCHAR(50) NOT NULL,
  DESCRIPTION  VARCHAR(255),
  FIRST_NAME   VARCHAR(30),
  LAST_NAME    VARCHAR(30),
  PHONE_NUMBER VARCHAR(20) NOT NULL,
  PRIMARY KEY (CLIENTS_ID)
);

CREATE TABLE PRODUCTS (
  ID            BIGINT NOT NULL AUTO_INCREMENT,
  ACTIVITY      CHAR(1),
  COLOUR        VARCHAR(30),
  GRADE         VARCHAR(20),
  INNER_HEIGHT  INT(11),
  INNER_LENGTH  INT(11),
  INNER_WIDTH   INT(11),
  PRINT         VARCHAR(30),
  PRODUCTS_NAME VARCHAR(200),
  PRODUCTS_TYPE VARCHAR(50),
  PROFILE       VARCHAR(10),
  CLIENTS_ID     BIGINT,
  PRIMARY KEY (ID),
  FOREIGN KEY (CLIENTS_ID) REFERENCES CLIENTS (CLIENTS_ID)
);


CREATE TABLE ORDERS (
  ID         BIGINT      NOT NULL AUTO_INCREMENT,
  CLIENTS_ID BIGINT      NOT NULL,
  SHIP_DATE  DATETIME    NOT NULL,
  STATUS     VARCHAR(20) NOT NULL,
  CONSTRAINT FK_ORDERS_CLIENTS FOREIGN KEY (CLIENTS_ID)
  REFERENCES CLIENTS (CLIENTS_ID),
  PRIMARY KEY (ID)
);

CREATE TABLE REQUESTS (
  ID             BIGINT       NOT NULL AUTO_INCREMENT,
  ORDER_ID       BIGINT       NOT NULL,
  PRODUCTS_LIST  VARCHAR(160) NOT NULL,
  READINESS_DATE DATETIME     NOT NULL,
  PRIMARY KEY (ID),
  CONSTRAINT FK_REQUESTS_ORDERS FOREIGN KEY (ORDER_ID) REFERENCES ORDERS (ID),
);

CREATE TABLE REQUESTS_PRODUCTS (
  REQUESTS_ID BIGINT NOT NULL,
  PRODUCTS_ID BIGINT NOT NULL,
  PRIMARY KEY (REQUESTS_ID, PRODUCTS_ID),
  CONSTRAINT REQUESTS_PRODUCTS_1 FOREIGN KEY (REQUESTS_ID) REFERENCES REQUESTS (ID)
    ON DELETE CASCADE,
  CONSTRAINT REQUESTS_PRODUCTS_2 FOREIGN KEY (PRODUCTS_ID) REFERENCES PRODUCTS (ID)
    ON DELETE CASCADE
);





CREATE TABLE CARDBOARD_BRAND (
  BRAND_ID   BIGINT(20)  NOT NULL AUTO_INCREMENT,
  BRAND      VARCHAR(200),
  PRIMARY KEY (BRAND_ID)
);
CREATE TABLE CARDBOARD_TYPE (
  CTYPE_ID   BIGINT(20)  NOT NULL AUTO_INCREMENT,
  CTYPE      VARCHAR(200),
  PRIMARY KEY (CTYPE_ID)
);
CREATE TABLE MANUFACTURER (
  MANUFACTURER_ID   BIGINT(20)  NOT NULL AUTO_INCREMENT,
  MANUFACTURER      VARCHAR(200),
  PRIMARY KEY (MANUFACTURER_ID)
);
CREATE TABLE PAPER_TYPE (
  PAPER_ID   BIGINT(20)  NOT NULL AUTO_INCREMENT,
  PAPER      VARCHAR(200),
  PRIMARY KEY (PAPER_ID)
);
CREATE TABLE PROFILE (
  PROFILE_ID   BIGINT(20)  NOT NULL AUTO_INCREMENT,
  PROFILE      VARCHAR(200),
  PRIMARY KEY (PROFILE_ID)
);
CREATE TABLE COMPOUND (
  COMPOUND_ID   BIGINT(20)  NOT NULL AUTO_INCREMENT,
  COMPOUND      VARCHAR(200),
  PRIMARY KEY (COMPOUND_ID)
);

CREATE TABLE TYPE_OF_PRODUCT (
  TYPE_OF_PRODUCT_ID   BIGINT(20)  NOT NULL AUTO_INCREMENT,
  TYPE_OF_PRODUCT      VARCHAR(200),
  PRIMARY KEY (TYPE_OF_PRODUCT_ID)
);

CREATE TABLE FORMAT (
  FORMAT_ID   BIGINT(20)  NOT NULL AUTO_INCREMENT,
  FORMAT      VARCHAR(200),
  PRIMARY KEY (FORMAT_ID)
);
CREATE TABLE FACE_LAYER (
  FACE_LAYER_ID   BIGINT(20)  NOT NULL AUTO_INCREMENT,
  FACE_LAYER      VARCHAR(200),
  PRIMARY KEY (FACE_LAYER_ID)
);
CREATE TABLE INNER_LAYER (
  INNER_LAYER_ID   BIGINT(20)  NOT NULL AUTO_INCREMENT,
  INNER_LAYER      VARCHAR(200),
  PRIMARY KEY (INNER_LAYER_ID)
);
CREATE TABLE CELLULOSIC_LAYER (
  CELLULOSIC_LAYER_ID   BIGINT(20)  NOT NULL AUTO_INCREMENT,
  CELLULOSIC_LAYER      VARCHAR(200),
  PRIMARY KEY (CELLULOSIC_LAYER_ID)
);
CREATE TABLE PACKING (
  PACKING_ID   BIGINT(20)  NOT NULL AUTO_INCREMENT,
  PACKING      VARCHAR(200),
  PRIMARY KEY (PACKING_ID)
);
CREATE TABLE PALLET (
  PALLET_ID   BIGINT(20)  NOT NULL AUTO_INCREMENT,
  PALLET      VARCHAR(200),
  PRIMARY KEY (PALLET_ID)
);
CREATE TABLE PLACEMENT (
  PLACEMENT_ID   BIGINT(20)  NOT NULL AUTO_INCREMENT,
  PLACEMENT      VARCHAR(200),
  PRIMARY KEY (PLACEMENT_ID)
);

