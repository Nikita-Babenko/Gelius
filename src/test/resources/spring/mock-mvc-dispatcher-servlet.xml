<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:jdbc="http://www.springframework.org/schema/jdbc"
       xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd
            http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.0.xsd
            http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
            http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc.xsd">

    <context:component-scan base-package="ua.skillsup.gelius" />

    <context:property-placeholder location="classpath:db/hibernate_test.properties, classpath:db/jdbc_test.properties"/>

    <mvc:annotation-driven />

    <jdbc:embedded-database id="GeliusEmbeddedDB" type="H2">
        <jdbc:script location="classpath:db/sql/create-db.sql" />
        <jdbc:script location="classpath:db/sql/insert-data.sql" />
    </jdbc:embedded-database>

    <bean class="org.modelmapper.ModelMapper" />

    <bean id="sessionFactoryTest" class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
        <property name="dataSource" ref="GeliusEmbeddedDB" />
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">${hibernate_test.dialect}</prop>
                <prop key="hibernate.connection.driver_class">${jdbc_test.driver}</prop>
                <prop key="hibernate.connection.url">${jdbc_test.url}</prop>
                <prop key="hibernate.show_sql">${hibernate_test.show_sql}</prop>
            </props>
        </property>
        <property name="packagesToScan" value="ua.skillsup.gelius.model.entity" />
    </bean>

    <bean id="transactionManager"
          class="org.springframework.orm.hibernate4.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactoryTest" />
    </bean>

    <tx:annotation-driven transaction-manager="transactionManager" />

</beans>